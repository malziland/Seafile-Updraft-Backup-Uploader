# macOS
.DS_Store

# Release artifacts
*.zip
/dist/
# rsync staging folder from the WP-zip build (same name as the plugin slug,
# created in the project root when the zip is built in-place rather than
# in /tmp/)
/seafile-updraft-backup-uploader/

# Composer
/vendor/
composer.phar

# PHPUnit / PHPStan / PHPCS caches
.phpunit.result.cache
.phpcs-cache
.phpstan-cache/

# IDE / editor
.idea/
.vscode/
*.swp
*.swo

# Claude Code session state
.claude/
